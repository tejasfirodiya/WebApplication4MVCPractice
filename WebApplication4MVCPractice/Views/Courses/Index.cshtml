@model IEnumerable<WebApplication4MVCPractice.Models.Course>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course_Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course_Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course_Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Course_Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course_Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course_Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Course_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Course_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Course_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
